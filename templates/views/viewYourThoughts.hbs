<!DOCTYPE html>
<html>
    <head>
        {{!-- <script src="/js/viewYourThoughts.js" defer></script> --}}
    </head>
        <body>
            <h3>Hi {{username}}!</h3>
            <p>This is where you can view your past thoughts</p>
            <p id="1">{{message}}</p>
            <a href="/uploadNewThought">{{link}}</a>
            <br><br>

           <script>
               console.log('script')
                fetch('/yourThoughts').then(response => {
                    response.json().then(data => {
                        for(let i=0; i<data.length; i++){
                            let header = document.createElement('h4')
                            header.textContent = data[i].header

                            let space = document.createElement('br')

                            let paragraph = document.createElement('p')
                            paragraph.textContent = data[i].date + ' ' + data[i].content

                            document.body.appendChild(header)
                            document.body.appendChild(paragraph)
                            document.body.appendChild(space)
                        }
                    })
                })            

           </script>
        </body>
</html>